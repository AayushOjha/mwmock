export enum ESectionType {
  product = 'product',
  faq = 'faq',
  links = 'links',
  review = 'review',
  card = 'card',
}

export enum ESectionName {
  product = 'Product Section',
  faq = 'F & Q',
  links = 'Quick Links',
  review = 'Review Section',
  card = 'Card Section',
}
