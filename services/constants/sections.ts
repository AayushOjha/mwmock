export enum ESectionType {
  product = 'product',
  faq = 'faq',
  links = 'links',
  review = 'review',
  card = 'card',
}

export enum ESectionName {
  product = 'Products',
  faq = 'F&Q',
  links = 'Links',
  review = 'Reviews',
  card = 'Cards',
}
